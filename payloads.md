Payload 1
При переходе по ссылке https://[gruyere-instance]/?q=test параметр q можно заменить на пейлоад <script>alert(document.cookie)</script> для выполнения Reflected XSS инъекции в элементе отображения результатов поиска, который покажет куки файлы пользователя.

Payload 2
При загрузке HTML файла через функционал upload можно создать файл с содержимым <script>alert(document.cookie)</script> для выполнения Stored XSS инъекции при просмотре загруженного файла, который покажет куки файлы пользователя.

Payload 3
При переходе по ссылке https://[gruyere-instance]/несуществующая_страница можно добавить в URL пейлоад <script>alert(document.cookie)</script> для выполнения Reflected XSS инъекции на странице ошибки 404, который покажет куки файлы пользователя.

Payload 4
При создании snippet можно в поле содержимого вставить пейлоад <script>alert(document.cookie)</script> для выполнения Stored XSS инъекции при просмотре snippet, который покажет куки файлы пользователя.

Payload 5
При изменении параметра цвета (например, в профиле) можно использовать пейлоад red"><script>alert(document.cookie)</script> для выполнения XSS инъекции путем выхода из атрибута style, который покажет куки файлы пользователя.

Payload 6
При редактировании профиля в поле имени можно вставить пейлоад <img src=x onerror=alert(document.cookie)> для выполнения Stored XSS инъекции при просмотре профиля, который покажет куки файлы пользователя.

Payload 7
При добавлении комментария можно использовать пейлоад <svg onload=alert(document.cookie)> для выполнения Stored XSS инъекции при просмотре комментариев, который покажет куки файлы пользователя.

Payload 8
При использовании функционала с вводом данных можно применить пейлоад <body onload=alert(document.cookie)> для выполнения XSS инъекции через event handler, который покажет куки файлы пользователя.

Payload 9
При создании ссылки можно использовать пейлоад <a href="javascript:alert(document.cookie)">Click</a> для выполнения XSS инъекции через JavaScript protocol, который покажет куки файлы пользователя при клике.

Payload 10
При попытке обхода фильтров можно использовать пейлоад <ScRiPt>alert(document.cookie)</ScRiPt> для выполнения XSS инъекции с использованием смешанного регистра, который покажет куки файлы пользователя.
